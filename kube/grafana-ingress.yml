apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: ${NAMESPACE}
  name: garion-grafana-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: grafana.${GITHUB_RUN_ID}.garion-telemetry-ts.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: garion-grafana-service
            port:
              number: 3000
  - host: grafana.${GITHUB_RUN_ID}.garion-kube.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: garion-grafana-service
            port:
              number: 3000